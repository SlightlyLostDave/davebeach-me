---
import '@styles/global.css';
import CV from '@cv';

interface Props {
  title: string;
}

const { title } = Astro.props;

const { summary } = CV.basics;
---

<!doctype html>
<html lang="en">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-PE7HD3B9NR"
    ></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());

      gtag('config', 'G-PE7HD3B9NR');
    </script>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />

    <title>{title}</title>

    <meta name="description" content={summary} />

    <link rel="icon" href="favicon.ico" type="image/x-icon" />
  </head>
  <body>
    <slot />
  </body>
</html>
